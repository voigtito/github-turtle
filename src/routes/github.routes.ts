import { Router, Request, Response } from 'express';

const githubRouter = Router();

githubRouter.get('/repositories', async (request:Request, response:Response) => {
  
});

export default githubRouter;
